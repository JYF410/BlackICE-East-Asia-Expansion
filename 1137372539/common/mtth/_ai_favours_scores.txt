
# scores for AI proposing favours
# ROOT = sender
# THIS = receiver

diplo_trade_bonus = {
	base = 1
	
	modifier = {
		factor = 0
		
		set_temp_variable = { resources = resource_exported@oil }
		add_to_temp_variable = { resource = resource_exported@rubber }
		add_to_temp_variable = { resource = resource_exported@steel }
		add_to_temp_variable = { resource = resource_exported@aluminium }
		add_to_temp_variable = { resource = resource_exported@chromium }
		add_to_temp_variable = { resource = resource_exported@tungsten }
		add_to_temp_variable = { resource = resource_exported@bauxite }
		add_to_temp_variable = { resource = resource_exported@coal }
		add_to_temp_variable = { resource = resource_exported@iron }
		
		check_variable = { resource < 150 }
	}
}

diplo_command_bonus = {
	base = 1
}

diplo_law_bonus = {
	base = 1
}

diplo_production_bonus = {
	base = 1
}

diplo_resource_bonus = {
	base = 1
	
	modifier = {
		factor = 0
		
		set_temp_variable = { resources = ROOT.resource_produced@oil }
		add_to_temp_variable = { resource = ROOT.resource_produced@rubber }
		add_to_temp_variable = { resource = ROOT.resource_produced@chromium }
		add_to_temp_variable = { resource = ROOT.resource_produced@tungsten }
		add_to_temp_variable = { resource = ROOT.resource_produced@bauxite }
		add_to_temp_variable = { resource = ROOT.resource_produced@coal }
		add_to_temp_variable = { resource = ROOT.resource_produced@iron }
		
		check_variable = { resource < 150 }
	}
}

diplo_experience_bonus = {
	base = 1
}

diplo_license_bonus = {
	base = 0.1
}

diplo_intelligence_bonus = {
	base = 1
	
	modifier = {
		factor = 0
		
		check_variable = { ROOT.agency_upgrade_number < 15 }
	}
	modifier = {
		factor = 0
		
		check_variable = { agency_upgrade_number < 25 }
	}
}
diplo_research_bonus = {
	base = 1
}
diplo_naval_bonus = {
	base = 1
	modifier = {
		factor = 0
		
		OR = {
			check_variable = { num_of_naval_factories < 7 }
			check_variable = { ROOT.num_of_naval_factories < 7 }
		}
	}
	modifier = {
		factor = 0
		OR = {
			NOT = { has_country_flag = coastal_state }
			ROOT = { NOT = { has_country_flag = coastal_state } }
		}
	}
}